version: '3.9'

services:
  postgres:
    restart: always
    image: docker.io/postgres:14.5
    container_name: dso-console_postgres
    environment:
      POSTGRES_DB: dso-console-db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin
    networks:
      - dso-network

  server:
    restart: always
    build:
      context: ..
      dockerfile: apps/server/Dockerfile
      target: prod
    image: dso/console/server
    container_name: dso-console_server
    depends_on:
      postgres:
        condition: service_started
      keycloak:
        condition: service_healthy
    ports:
      - 4000:4000
    env_file:
      - ../apps/server/env/.env
    # volumes:
    #   - ../apps/server/env/.env:/app/.env
    environment:
      NODE_ENV: production
    networks:
      - dso-network

  client:
    build:
      context: ..
      dockerfile: apps/client/Dockerfile
      target: prod
    image: dso/console/client
    container_name: dso-console_client
    depends_on:
      server:
        condition: service_started
    ports:
      - 8080:8080
    # volumes:
    #   - ../apps/client/env/.env:/docker-entrypoint.d/.env
    env_file:
      - ../apps/client/env/.env
    environment:
      NODE_ENV: production
    networks:
      - dso-network

networks:
  dso-network:
    name: dso-network
