# À propos

Ce nginx est celui utilisé à fin d'exposer les fichiers de l'application "SPA" (Single Page Application) de la Console.

**À ne pas confondre avec [`nginx-strangler`](../../nginx-strangler) qui est le composant permettant le transfert progressif des routes de `server` vers `server-nestjs`**
