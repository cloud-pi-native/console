kind: ConfigMap
apiVersion: v1
metadata:
  name: {{ $.Release.Name }}-client-app-cm
data: 
  .env: |-
    {{- with .Values.keycloak }}
    KEYCLOAK_PROTOCOL=https
    KEYCLOAK_DOMAIN={{ .domain }}
    KEYCLOAK_REALM={{ .realm }}
    KEYCLOAK_CLIENT_ID={{ .client_id_frontend }}
    KEYCLOAK_REDIRECT_URI={{ .redirect_uri }}
    {{- end }}
