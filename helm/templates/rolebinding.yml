apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: {{ $.Release.Name }}-clusterole-binding
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: {{ $.Release.Name }}-ansible-cluster-role
subjects:
- kind: ServiceAccount
  name: {{ $.Release.Name }}-ansible-sa
  namespace: {{ $.Release.Namespace }}